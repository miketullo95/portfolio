import type { CaseStudyMeta } from '@/lib/types'

// TODO: Build case study header (title, subtitle, meta: role, timeline, company)
export default function CaseStudyHeader({ meta }: { meta: CaseStudyMeta }) {
  return <header />
}
